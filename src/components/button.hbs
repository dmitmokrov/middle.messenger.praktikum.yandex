{{#if (eq tag "a")}}
  <a class="button {{#if (eq type "secondary")}} button--secondary {{/if}}" href={{ href }}>{{ text }}</a>
{{else}}
  <button class="button {{#if (eq type "secondary")}} button--secondary {{/if}}" type="button">{{ text }}</button>
{{/if}}